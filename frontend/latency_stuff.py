    # end_latency_time = time.time()
    # latency = end_latency_time - start_latency_time

    # st.write(response.text)
    # st.write(response.content)

    # Initialize a variable to keep track of the total reviews count
    # total_reviews = 0
    # total_photos = 0

    # Iterate over all places and sum the length of their reviews
    # for place in data['places']:
    #     total_reviews += len(place['reviews'])
    #     total_photos += len(place['photos'])

    # st.write(f'location: {float(st.session_state['validated_location'][0])}, {float(st.session_state['validated_location'][1])}')
    # st.write(f'# reviews: {total_reviews}')
    # st.write(f'# Photos: {total_photos}')
    # st.write(f'Latency: {latency}')